# Scamly Beta AI launch bundle

- `extension/` Chrome extension
- `backend/` server for AI analysis + privacy/support/feedback pages
- `docs/` launch guidance and store copy
- `tests/` local verification
- `store_assets/` screenshots

Recommended repo layout: upload this whole bundle to GitHub and point Render at the `backend/` directory.

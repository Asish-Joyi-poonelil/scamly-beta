# Scamly AI backend

This service does three jobs:
1. serves your public privacy/support/feedback pages
2. runs Deep AI Check through OpenAI from a secure server-side key
3. stores beta feedback in Supabase when configured

Use `.env.example` as the template for your environment variables.
